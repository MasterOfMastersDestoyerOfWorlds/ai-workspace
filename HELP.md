# ixd help

Generated by `ixd docs`; every word below comes from a command's docstring and signature.

```
usage: ixd [-h] command ...

Workspace commands: worktrees, landing, machine setup and transcript statistics.

positional arguments:
  command
    docs      Write the command reference into README.md and HELP.md from the
              docstrings.
    land      Merge a worktree's change into the main branch and clean up
              after it.
    setup     Clone the repositories, install the commands, record REPO_HOME,
              install the autofix artifact.
    stats     Print wall time by activity, thinking pauses, slow calls, errors
              and repeated command shapes.
    wt        abort, commit, continue, done, launch-add, launch-list, new,
              status, sync

options:
  -h, --help  show this help message and exit
```
## ixd docs
```
usage: ixd docs [-h] [--check]

Write the command reference into README.md and HELP.md from the docstrings.

README.md gets a table and one section per command, between the generated-region markers, so the
prose around it survives. HELP.md gets every parser's help exactly as a terminal prints it. Both
come from the same decorators the CLI itself is built from, so neither can drift.

options:
  -h, --help  show this help message and exit
  --check     report whether the files are current and change nothing, exiting
              1 when they are stale
```
## ixd land
```
usage: ixd land [-h] [-m MESSAGE] [--body {none,changes,description}]
                [--keep-launch] [--keep-worktree] [--no-build] [--no-mark]
                [worktree_name]

Merge a worktree's change into the main branch and clean up after it.

Six steps, each refusing to continue on failure. Replay the change onto the current main branch.
Restore the main branch's .vscode/launch.json, because the entries an agent added are
verification aids rather than part of the change. Squash the diff into one commit. Fast-forward
the main checkout. Remove the worktree and its branch. Mark the branch's ticket DONE, which
happens after the removal so the ticket CLI's unmerged-worktree guard sees the landed state.

The main checkout is then recompiled: the VS Code Java extension does not notice files git
changed underneath it, so the next F5 would otherwise run pre-merge classes and answer HTTP 404
for routes that now exist.

positional arguments:
  worktree_name         worktree path or bare name, defaulting to the one
                        holding the current directory

options:
  -h, --help            show this help message and exit
  -m MESSAGE, --message MESSAGE
                        override the commit message entirely, instead of
                        building it from the ticket
  --body {none,changes,description}
                        commit body: the subject alone, the ticket's changes-
                        made bullets, or its description
  --keep-launch         keep the worktree's launch.json changes instead of
                        restoring the main branch's
  --keep-worktree       merge but leave the worktree and branch in place
  --no-build            skip recompiling the main checkout after the merge
  --no-mark             leave the ticket's status alone instead of marking it
                        DONE
```
## ixd setup
```
usage: ixd setup [-h] [--repo-home REPO_HOME] [--dry-run] [--no-shell]
                 [--no-tools] [--no-maven]

Clone the repositories, install the commands, record REPO_HOME, install the autofix artifact.

Every step is idempotent, so rerunning after a partial failure is safe. REPO_HOME defaults to the
directory holding this checkout, because the other repositories are its siblings. It is written to
.claude/settings.local.json for Claude sessions and to the shell profile for terminals.

options:
  -h, --help            show this help message and exit
  --repo-home REPO_HOME
                        where the repositories live, defaulting to the parent
                        of this checkout
  --dry-run             print every step without running it
  --no-shell            do not touch the shell profile or the user environment
  --no-tools            skip installing the commands and syncing the
                        environment
  --no-maven            skip installing the autofix artifact into ~/.m2
```
## ixd stats
```
usage: ixd stats [-h] [--session SESSION] [--latest LATEST] [--top TOP]
                 [--idle IDLE]
                 [transcript ...]

Print wall time by activity, thinking pauses, slow calls, errors and repeated command shapes.

This is the evidence /ix-tool-review reads first: the biggest number in the activity table is
usually the biggest finding. A session id pulls in that session and every subagent it spawned.

positional arguments:
  transcript         transcript paths to read, when neither a session nor a
                     recent count is given

options:
  -h, --help         show this help message and exit
  --session SESSION  session id, which also pulls in its subagents
  --latest LATEST    read the N most recent sessions with their subagents
  --top TOP          rows to print per list
  --idle IDLE        seconds of gap after a result that count as idle rather
                     than activity
```
## ixd wt
```
usage: ixd wt [-h] subcommand ...

wt: abort, commit, continue, done, launch-add, launch-list, new, status, sync

positional arguments:
  subcommand
    abort      Abandon an interrupted sync and restore the state the worktree
               was in before it.
    commit     Make one squashed commit of the whole diff on top of the main
               branch.
    continue   After the conflict markers are gone, finish the interrupted
               sync.
    done       Sync, build, run the launch entry, check tmp/ holds a
               screenshot, then mark the ticket REVIEW.
    launch-add
               Add one entry to the worktree's .vscode/launch.json, keeping
               the file's comments.
    launch-list
               Print the launch entry names this worktree offers, with their
               scene arguments.
    new        Create the worktree and branch off the main branch, seed it,
               and print the agent brief.
    status     Show the branch, its distance from main, the changed files, and
               the last agent note.
    sync       Rebase the uncommitted change onto the main branch and leave it
               uncommitted again.

options:
  -h, --help   show this help message and exit
```
### ixd wt abort
```
usage: ixd wt abort [-h] [worktree_name]

Abandon an interrupted sync and restore the state the worktree was in before it.

positional arguments:
  worktree_name  worktree path or bare name, defaulting to the one holding the
                 current directory

options:
  -h, --help     show this help message and exit
```
### ixd wt commit
```
usage: ixd wt commit [-h] -m MESSAGE [worktree_name]

Make one squashed commit of the whole diff on top of the main branch.

Requires a fresh sync, so the result is a single commit the user can fast-forward: no merge
commits and no history to read.

positional arguments:
  worktree_name         worktree path or bare name, defaulting to the one
                        holding the current directory

options:
  -h, --help            show this help message and exit
  -m MESSAGE, --message MESSAGE
                        the commit message
```
### ixd wt continue
```
usage: ixd wt continue [-h] [worktree_name]

After the conflict markers are gone, finish the interrupted sync.

Refuses while any tracked file outside tmp/ still holds a marker, and refuses when a file git
listed as unmerged has vanished from the working tree.

positional arguments:
  worktree_name  worktree path or bare name, defaulting to the one holding the
                 current directory

options:
  -h, --help     show this help message and exit
```
### ixd wt done
```
usage: ixd wt done [-h] [--skip-launch] [worktree_name]

Sync, build, run the launch entry, check tmp/ holds a screenshot, then mark the ticket REVIEW.

Every step must pass. A failure stops with the reason and nothing is marked, because a ticket in
REVIEW is a claim that the user can verify the work with F5.

positional arguments:
  worktree_name  worktree path or bare name, defaulting to the one holding the
                 current directory

options:
  -h, --help     show this help message and exit
  --skip-launch  do not run the launch entry, though the screenshot check
                 still applies
```
### ixd wt launch-add
```
usage: ixd wt launch-add [-h] --scene SCENE [--property PROPERTY]
                         [--name NAME]
                         [worktree_name]

Add one entry to the worktree's .vscode/launch.json, keeping the file's comments.

The file is edited as JSONC, so comments a human left survive. The result is parsed before it is
written, so a broken edit never reaches disk. `ixd land` strips these entries before merging:
they are verification aids, not part of the change.

positional arguments:
  worktree_name        worktree path or bare name, defaulting to the one
                       holding the current directory

options:
  -h, --help           show this help message and exit
  --scene SCENE        scene id passed to IxdarWindow
  --property PROPERTY  JVM system property written as -DKEY=VALUE, repeatable
  --name NAME          entry name, defaulting to "TICKET: scene"
```
### ixd wt launch-list
```
usage: ixd wt launch-list [-h] [worktree_name]

Print the launch entry names this worktree offers, with their scene arguments.

positional arguments:
  worktree_name  worktree path or bare name, defaulting to the one holding the
                 current directory

options:
  -h, --help     show this help message and exit
```
### ixd wt new
```
usage: ixd wt new [-h] [--repo {Ixdar,ixdar-tickets,ai-workspace}] ticket

Create the worktree and branch off the main branch, seed it, and print the agent brief.

The branch takes the ticket's name, which is how `ixd wt done` and `ixd land` find the ticket to
mark. The virtualenv is created up front so an agent's first command is not a cold install. The
brief that is printed is everything an agent needs to start: the paths, the ticket file, the
verbs, and what is denied.

positional arguments:
  ticket                worktree and branch name, usually the ticket id such
                        as craw-27

options:
  -h, --help            show this help message and exit
  --repo {Ixdar,ixdar-tickets,ai-workspace}
                        repository under REPO_HOME that gets the worktree
```
### ixd wt status
```
usage: ixd wt status [-h] [worktree_name]

Show the branch, its distance from main, the changed files, and the last agent note.

This is what a relaunched agent reads instead of a hand-written brief. The note is the closing
prose of the transcript whose tool calls actually worked in this worktree, so an agent picking
the work up learns what was built and what was left.

positional arguments:
  worktree_name  worktree path or bare name, defaulting to the one holding the
                 current directory

options:
  -h, --help     show this help message and exit
```
### ixd wt sync
```
usage: ixd wt sync [-h] [worktree_name]

Rebase the uncommitted change onto the main branch and leave it uncommitted again.

Work is never lost: the change is parked in a temporary commit, replayed, then unpacked. A real
conflict leaves the rebase in progress with the files listed, for `continue` or `abort`.

positional arguments:
  worktree_name  worktree path or bare name, defaulting to the one holding the
                 current directory

options:
  -h, --help     show this help message and exit
```
